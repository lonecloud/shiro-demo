#当设置多Relam 的时候有个生成策略:
#1.众多的realm只要有一个成功就成功FirstSuccessfulStrategy 返回成功的realm认证信息
#2.AtLeastOneSuccessfulStrategy,这个是返回所有的Realm的认证信息
#3.AllSuccessfulStrategy是所有的成功后才进行返回只要有一个失败了就不会执行下一个
#指定securityManager的authenticator实现
authenticator=org.apache.shiro.authc.pam.ModularRealmAuthenticator
securityManager.authenticator=$authenticator

#指定securityManager.authenticator的authenticationStrategy
allSuccessfulStrategy=org.apache.shiro.authc.pam.AtLeastOneSuccessfulStrategy
securityManager.authenticator.authenticationStrategy=$allSuccessfulStrategy
#声明一个realm
MyRealm=cn.lonecloud.realm.MyRealm
MyRealm2=cn.lonecloud.realm.MyRealm2
#指定securityManager的realms实现
securityManager.realms=$MyRealm,$MyRealm2